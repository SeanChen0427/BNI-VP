# 2026-09-12｜v1.8.2 訪談表單直接選課

來源：Sean 要求新會員與續約訪談可直接選官方培訓場次，不再自行查課後手填時間。

## 發布內容

- 新會員 MSP 上／下、交流座談會首選／備選，續約 MSP 上／下，共六個日期欄位接上同一份官方課程庫。
- 選課帶入台北日期，官方 ID 與當時名稱、時間保存至既有案件草稿；相容舊手填日期及正式 Word 日期欄位。
- 已選場次改期、改名或暫未列出時提示確認；讀取失敗保留原日期與場次，手動改日期解除關聯。
- 手機顯示完整已選課程時間，修正表單最小寬度造成的水平溢出。
- 分類：既有表單操作改善 `fix`，v1.8.1 → v1.8.2。

## 驗證

- 總專案 `npm run check`：304 項測試通過、0 失敗；專案及跨模組稽核 0 錯誤。保留原有 20 項 CSS 單行格式改善提醒，沒有新警告。
- 官方 BNI baseline：46／46 會員逐項一致。
- 瀏覽器使用隔離虛構案件與課表，實際執行兩份表單的初始化及保存／恢復程式；四個新會員欄位及兩個續約欄位均可帶入日期與保存場次。
- 驗證官方改期時保留舊日期、人工採用最新時間、missing 場次保留、HTTP 503 保留選課、手動輸入解除關聯。
- 390px 手機與 1280px 桌面無水平溢出，已選課程完整時間另列於選單下方。
- 正式登入後的會員草稿寫入未以真實案件做自動測試；沿用既有 `task_case_states.draft` JSON 與案件權限，無後端部署或 migration。

## 發布及復原

以既有 GitHub Pages main 發布流程送出，只包含本次前端與文件變更。部署 workflow、commit 與線上靜態檔比對結果保存於 Git 忽略的 `data/deployments/2026-09-12-training-selection-v182.json`；該成功紀錄為正式發布完成依據。

如需回退前端可回退本次程式提交；已保存的選課快照留在原草稿 JSON，舊版仍可讀取原日期。課表、每日排程及會員完成紀錄不受影響。
