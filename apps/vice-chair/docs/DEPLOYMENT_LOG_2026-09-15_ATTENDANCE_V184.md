# 2026-09-15｜v1.8.4 點名全未勾選漏列缺席修正

來源：Sean 回報當日點名全未勾選卻未列缺席，並明確授權「更新上架」。

## 發布內容

- 分類 `fix`：v1.8.3 → v1.8.4。未完成簡報且無代理者包含全未勾選，均列入本週作業缺席及公告累計。
- 前端與 Supabase `app-api` 共用 `attendance-domain.js`；前端更新快取版本與判定說明，後端同步部署。既有已確認公告快照保留，更正仍走重新開啟及雙重確認。
- 以正式 main `504023751e7870d6e1d4979cc5227ac32aa387ae` 的隔離副本加入本次修正，保留主工作目錄既有未提交修改及 Git index。

## 驗證

- 正式 2026-09-15 點名頁已唯讀重現回報症狀；測試與公開文件只使用去識別資料。
- 完整 `npm run check`：304 項通過、0 失敗，工作台與跨模組稽核 0 錯誤，20 項既有 CSS 格式改善提醒；BNI 官方回歸 46/46。
- 回歸涵蓋前後端欄位、代理／簡報例外、累計、公告預覽補勾更新與鎖定快照保留。升版同步更新既有版本測試。
- 公開前端打包 191 檔；發布前執行公開 repository 掃描。測試使用 Git 忽略的本機私密復原資料連結，沒有加入發布清單。
- 正式 app-api 原版本 v85，已核對原入口原始碼與正式 main 相同；CLI 完整下載遇到跨目錄 import 解包限制，保留入口備份、原版本 metadata 與可重部署的正式 main，未宣稱完整線上原始碼已備份。

## 發布與復原

先部署 Supabase `app-api` 並確認 ACTIVE、JWT 驗證與匿名請求拒絕，再推送 GitHub Pages main。最終 commit、Pages workflow、函式版本及線上檔案雜湊比對保存於 Git 忽略的 `apps/vice-chair/data/deployments/2026-09-15-attendance-v184.json`；該成功紀錄為發布完成依據。

未改寫正式點名、PALMS 或已確認公告，未自動確認或發送 LINE。若需復原，以前版 main 還原前端並重新部署前版 app-api；保留所有正式資料與操作歷程。
