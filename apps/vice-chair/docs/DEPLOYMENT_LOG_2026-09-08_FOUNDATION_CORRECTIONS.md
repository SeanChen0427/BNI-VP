# 2026-09-08｜v1.3.1 地基日期修正、刪除及位置調整

來源：Sean 回報既有地基起算日無法修正且不能刪除，並追加首頁改在待處理事項下方。沿用先前正式站試用授權；教學未更新。

## 變更與資料處理

- 副主席／Admin 可以修正既有起算日及期限；依新期間重新核對數據，只沿用完整起訖相同的工作坊確認。前後條件、受影響確認及原提醒歷程保留。
- 可逐項刪除並從副主席專用已刪除清單復原，不影響同會員其他地基。首頁、訪談現行追蹤與月會新快照由後端排除已刪除紀錄；舊 Word／月會封存不改寫。
- deletedAt／deletedBy／statusBeforeDelete 存在原 data JSON，維持既有 CAS 交易及事件表。刪除／復原使用既有 resolve／reopen RPC 事件種類，detail.operation 明確區分操作。無 migration 或批次正式會員資料修改。
- 首頁地基區塊位於 #cases 待辦案件清單正下方、每月資料更新之前。

## 驗證

- 完整 npm run check：254 passed、0 failed；稽核 0 錯誤、20 項既有 CSS 改善提醒；BNI 官方逐人逐項 46/46 相符。
- 地基相關 25 項測試，新增日期修正／有效範圍／確認失效與沿用、刪除及復原各狀態、非管理者拒絕、月會及訪談排除、同會員其他項目保留、版本衝突及稽核前後快照。
- 合成本機瀏覽器已完成：起算日 1/1 修正為 2/1 並保存、刪除單項、已刪除清單復原、回到列管後仍沿用 2/1。沒有用正式會員做寫入測試。
- 公開前端 164 檔；公開 repository 掃描仍為三份歷史文件姓名警示（3／6／1），本次新增行無會員姓名，歷史警示數量不變。
- 根目錄 AGENTS.md／CLAUDE.md 在本次開始前已有使用者變更，保留並排除本次提交。

## 回復

部署前版本 1c219b4。正式紀錄不刪除，歷程可核對。若已有 deletedAt 紀錄，回復介面時仍須保留後端刪除過濾與復原契約，避免舊版再次把已刪除項目帶入訪談；不以清除正式資料達成退版。

部署結果完成後追加。

## 正式部署完成

- app-api 已由 Supabase CLI 部署成功，沒有變更 JWT、RLS 或資料表權限。
- 功能提交 `59dc3ff342f52bf2c4fbf15dc18fe396731c418f` 已推送 main；GitHub Pages run `34146799299` 完成且 success。
- 正式 6 個相關 HTML／JS 與驗證產物一致；匿名 API 仍回 HTTP 401。
- 使用既有副主席工作階段只讀驗證：首頁 #cases 下一個區塊是續約地基追蹤；正式地基頁資料正常載入、每項具刪除入口、編輯表單起算日非唯讀且可操作。開啟後取消，沒有儲存或刪除正式會員資料。
- 合成資料的修改／刪除／復原已實際完成；正式站本次只讀檢查，不宣稱已做正式資料寫入演練。
