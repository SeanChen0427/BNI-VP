# 2026-09-18｜v1.8.6 中心區公版訪談與舊版副本

來源：Sean 指定中心區公版格式，確認副本涵蓋已有 Word 的回饋／投票中及已結案案件，並授權「部署上線」。

## 發布範圍

- 分類 `fix`：修正既有訪談格式及另存副本操作，v1.8.5 → v1.8.6。
- 四份私密公版對應新會員、轉換行業別、期中、續約與離會訪談；前後端核對來源與模板 SHA-256。公版原檔及私密 JSON 不進 GitHub。
- 已保存舊版 Word 的案件，不限是否結案，可由副主席／Admin 另下載公版副本。已有系統公版標記的附件不顯示按鈕。副本標示待核對，保存填答與原 Word 的差異須對照原件確認。
- 原訪談附件、回饋、票數、結案與案件歷程保留；不批次重製歷史、不改寫正式案件，不向 LINE 發送訊息。
- 從正式 main `89c8399` 建立隔離發布副本，主工作區其他未提交內容及 Git index 保留。

## 發布前驗證

- 隔離發布副本 `npm run check`：320 項測試通過、0 失敗；稽核 0 錯誤、20 項既有 CSS 格式提醒；BNI 官方回歸 46/46。
- 公開前端打包 197 檔；公開 repository 掃描通過。私密回歸資料僅以 Git 忽略的本機連結供驗證，不列入發布。
- 合成 Chrome 案例涵蓋回饋中、投票中、已結案的舊版及新公版：按鈕顯示與隱藏正確，副本下載前後案件保存資料逐字一致。四種模板已逐頁渲染驗證；本次重製會員副本四頁亦已檢查。
- 正式 app-api 原版本 v86；下載的原入口與正式 main 完全相同。CLI 跨目錄 import 解包限制使完整下載失敗，保留已下載入口、版本 metadata 與可重部署的正式 main，不宣稱已完整備份線上原始碼。

## 部署順序與復原

先以單一 transaction 套用 `20260918090000_private_form_templates`，建立非公開 `form-templates` bucket 並記錄 migration；再上傳四份模板、核對私密物件雜湊、部署 app-api，最後發布 GitHub Pages。

正式發布完成依據為 Git 忽略的 `apps/vice-chair/data/deployments/2026-09-18-interview-templates-v186.json`：記錄 commit、Pages workflow、函式版本、三角色模板 API、匿名／Storage 拒絕、五類合成產檔、線上檔案雜湊及正式資料前後摘要。未完成該證據前，不以本文件宣稱線上已生效。

若需復原，以 v1.8.5 正式 main 重新部署 app-api，前端以本版的反向提交還原。私密模板 bucket 可保留為未使用資源；不刪除歷史附件、會員或投票資料。

## 正式部署結果

2026-09-18 已發布 v1.8.6：前端 commit `73fd1878a6f0687c73dc5d5b60445699060027ed`，GitHub Pages workflow `35256835584` 成功；24 份線上頁面與程式 SHA-256 與發布內容一致。app-api v87 為 ACTIVE，JWT 驗證保持啟用；四份私密模板雜湊全部吻合。匿名模板 API 回 401，公開 Storage 讀取被拒絕。

正式案件、附件索引、案件狀態、投票及回饋五份資料摘要與發布前完全相同。部署與原件保留已核對；登入後的三角色端到端驗證尚未完成：本機保存憑證已失效，管理端建立測試登入的操作被自動核准審核拒絕且未執行，待 Sean 明確授權或自行登入驗收。不得將本次結果描述為三角色線上驗證全數通過。
