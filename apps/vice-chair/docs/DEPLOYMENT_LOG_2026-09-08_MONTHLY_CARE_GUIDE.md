# 2026-09-08｜v1.3.2 月會不關懷結案與地基導覽

## 範圍與授權

Sean 回報已表明不續約的會員仍被強制安排關懷，導致月會無法結案；並要求補上既有頁面教學缺少的續約地基追蹤。沿用本任務先前「推上去」的正式發布授權。

- 期中及特定會員關懷新增 `no_follow_up` 決議，前後端正規化為本次已處理、免委員與日期，備註選填。更新名單保留決議；既有工作不會被新增、刪除或重開，會員資格不變。
- 草稿可改回追蹤並恢復分工要求。續約的「確認不續約」及結案後追加更正維持原流程。
- 補上主選單、首頁地基區與追蹤頁導覽；同步月會、期中及續約訪談的地基說明。副主席追蹤頁共 14 步，委員依權限過濾管理動作。制度課程維持既有內容。
- 不修改正式會員個案、不代為結案正式月會，不涉及 SQL migration 或 BNI 計分修改。

## 驗證

- 完整 `npm run check`：260 項測試通過、0 失敗；稽核 0 錯誤、20 項既有 CSS 格式提醒；BNI 官方對帳 46／46。
- 新增回歸涵蓋前後端一致、限定關懷類型、恢復追蹤、重載與來源消失時保留決議、摘要及既有工作。以正式 API 原始函式搭配合成資料庫驗證免關懷可寫入正式結案狀態，而缺分工的需追蹤項目仍拒絕結案。
- Chrome 合成資料頁逐一完成 14 步地基導覽；月會驗證新選項、免必填、草稿保存、重新載入及備註保留，成功進入結案確認提示。
- `prepare-github-pages.mjs` 產出 164 個公開前端檔案。發布前 diff 檢查與根目錄 AGENTS／CLAUDE 一致性通過。公開 repository 掃描仍為原有 3 份歷史文件姓名提示（3／6／1），本次新增內容與新檔沒有會員姓名命中。

## 發布與相容回復

發布結果於正式部署完成後追加。前端與 app-api 必須一同更新；無資料庫 migration。前版程式參考 `b119882`（v1.3.1）。若已保存新決議，不可直接降回不認識 `no_follow_up` 的舊後端；應保留新決議的讀取、驗證及免分工相容邏輯，再個別回復畫面或教學。

## 正式發布完成

- 產品提交：`91db74b1ee4ce9730f6d98398d3459080033e509`（v1.3.2）。
- Supabase `app-api` 已成功部署至 `fahrblkukuhgveiptufn`，CLI 回傳 `Deployed Functions.`。
- GitHub Pages [發布流程 34148788566](https://github.com/SeanChen0427/BNI-VP/actions/runs/34148788566) 成功，提交與上述版本一致。
- 正式網址唯讀核對 8 個主要 HTML／JS 資產，與本機發布產物逐位元組相符；匿名會員 API 仍回傳 401。
- 合成測試分頁與本機測試伺服器已關閉。根目錄 AGENTS.md、CLAUDE.md 原有未提交變更完整保留。
